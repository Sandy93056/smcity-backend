from tracker.models import AddDeviceTracker, CreateUserTracker
from django.contrib import admin

# Register your models here.
admin.site.register(CreateUserTracker)
admin.site.register(AddDeviceTracker)
